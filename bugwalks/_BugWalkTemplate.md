# BugWalk Board

**Last updated:** YYYY-MM-DD  
**Board owner:** <team or repo>  
**Primary source capture:** `bugwalks/YYYY-MM-DD-<slug>.mht`  
**Operating rule:** `BugWalkBoard.md` is the live walkthrough board. `docs/CurrentState.md` is the canonical implementation log. Every bug-fix pass ends with updates to both files.

## Fresh Sightings

### BW-YYYY-MM-DD-01 — Short title

- `Status:` Fresh Sightings
- `Surface:` <page, route, component, or flow>
- `Symptom:` <what the user experienced>
- `Impact:` <why it matters>
- `Source:` <capture date or walkthrough name>
- `Owner:` <team or person>
- `Next move:` <next concrete action>
- `Evidence:` <files, capture names, links>

## Ready / Confirmed

### BW-YYYY-MM-DD-02 — Short title

- `Status:` Ready / Confirmed
- `Surface:` <page, route, component, or flow>
- `Symptom:` <what the user experienced>
- `Impact:` <why it matters>
- `Source:` <capture date or walkthrough name>
- `Owner:` <team or person>
- `Next move:` <next concrete action>
- `Evidence:` <files, capture names, links>

## In Flight

### BW-YYYY-MM-DD-03 — Short title

- `Status:` In Flight
- `Surface:` <page, route, component, or flow>
- `Symptom:` <what the user experienced>
- `Impact:` <why it matters>
- `Source:` <capture date or walkthrough name>
- `Owner:` <team or person>
- `Next move:` <next concrete action>
- `Evidence:` <files, capture names, links>
- `Attempt log:` YYYY-MM-DD — Tried <change>. Result: <fixed | partially fixed | still reproduces>.
- `CurrentState link:` <path or pending same-pass write-up>

## Shipped / Verify

### BW-YYYY-MM-DD-04 — Short title

- `Status:` Shipped / Verify
- `Surface:` <page, route, component, or flow>
- `Symptom:` <what the user experienced>
- `Impact:` <why it matters>
- `Source:` <capture date or walkthrough name>
- `Owner:` <team or person>
- `Next move:` <what still needs checking>
- `Evidence:` <touched files>
- `Validation:` <command or manual check>
- `CurrentState link:` <path or pending>
- `Attempt log:` YYYY-MM-DD — Tried <change>. Result: <appears fixed | needs more verification>.

## Watching / Deferred

### BW-YYYY-MM-DD-05 — Short title

- `Status:` Watching / Deferred
- `Surface:` <page, route, component, or flow>
- `Symptom:` <what the user experienced>
- `Impact:` <why it matters>
- `Source:` <capture date or walkthrough name>
- `Owner:` <team or person>
- `Next move:` <blocking dependency or defer reason>
- `Evidence:` <files, capture names, links>
- `Attempt log:` YYYY-MM-DD — Tried <change>. Result: <still reproduces>.
- `Persistence note:` <what did not work and what to check next>
- `CurrentState link:` <path or pending same-pass write-up>

# TODO

## Active queue

- [ ] Fill the expert learning tree with the current runtime map.
- [ ] Keep the provider registry aligned with `openai.yaml`.
- [ ] Keep `agents/INDEX.md` in sync with the expert spec.
- [ ] Use `assets/gestaltview-expert/` for refactor artifacts generated during
  deep dives.

## Rules

- Limit the active queue to the next three actions.
- Promote a TODO to code only when the target file and acceptance criteria are
  known.
- Mark completed items with the date and the validation that closed them.

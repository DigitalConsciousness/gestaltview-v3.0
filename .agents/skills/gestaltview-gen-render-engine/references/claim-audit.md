# Claim audit

| Date | Claim | Evidence | Status |
|---|---|---|---|
| 2026-07-17 | The static `NodeType` union in rooted `shared/rendering/engine/core/types.ts` declares 29 node types, while the operational rooted validator in `shared/rendering/engine/core/validation.ts` admits only 14. The 15 static-only types are `Slide`, `MindMap`, `Image`, `Video`, `Audio`, `Wiki`, `App`, `Component`, `AgentStudio`, `Storybook`, `Canvas`, `Pitch`, `Brand`, `Prompt`, and `Table`. To avoid claiming unsupported operational behavior, the Python helper follows the 14 types admitted by rooted `validation.ts`; it rejects the 15 static-only types. The rooted static/runtime mismatch remains unresolved, is outside this skill-only task, and must not be claimed fixed. The static and operational rooted contracts agree on all seven edge types: `contains`, `referencesAsset`, `rendersTo`, `derivedFrom`, `controls`, `annotates`, and `composes`. | Complete inspection of rooted `shared/rendering/engine/core/types.ts` and `shared/rendering/engine/core/validation.ts`; source-derived operational parity and static-drift coverage in `.agents/skills/gestaltview-gen-render-engine/tests/test_scene_graph_validator.py`. | **Partial:** Python helper matches rooted operational validation; 15 static node types remain unsupported at runtime. |
| 2026-07-17 | The rooted API parser and rooted shared types expose two distinct target shapes. `api/render/request.ts` accepts strict request targets containing `format`, `mimeType`, optional `width`/`height`/`quality`, and defaultable `destinationIntent`/`required`; this corresponds to shared `RenderTarget`. The older shared `ExportTarget` instead requires `uri` and does not contain `mimeType` or `destinationIntent`. The mind-map helper's outer request targets follow the current API parser, while its `ExportRequest.props.targets` remain a minimal scene-node instruction (`format`/`required`) because no rooted static type defines that props shape. | Complete inspection of rooted `api/render/request.ts` and `shared/rendering/engine/core/types.ts`; tests import and execute `parseRequestBody` through Node's TypeScript loader and prove that generated default and custom requests round-trip without further parser normalization. Rejection tests cover helper-side target and idempotency constraints in `.agents/skills/gestaltview-gen-render-engine/tests/test_mindmap_builder.py`. | **Partial:** direct-parser outer-envelope parity is verified locally for the helper's generated default and custom scene-graph requests; this is not a claim about other source families. The embedded `ExportRequest.props.targets` shape is untyped by the rooted contract and must not be represented as API-target parity. |
| 2026-07-17 | The mind-map builder enforces helper-specific resource limits before generation: 1 MiB input, 4,096 title characters, 256 graph/item-ID characters, 4,096 label characters, 1,000 branches, 1,000 children per branch, 4,000 total mind-map nodes, and 500,000 generated Mermaid characters. These are local denial-of-service protections, not rooted API limits. | Boundary and over-bound coverage, bounded binary input reading, and a 10,000-collision allocator work-count regression in `.agents/skills/gestaltview-gen-render-engine/tests/test_mindmap_builder.py`. | **Verified locally:** helper safety contract only; no claim that the rooted API independently enforces these limits. |
| 2026-07-17 | The submission helper uses the rooted `POST /api/render/engine` and `GET /api/render/status?jobId=...` endpoints with bearer authentication and JSON bodies. It recognizes the rooted `{ ok, job, ... }` response envelope and the `ready`, `failed`, and `cancelled` terminal states. | Complete inspection of rooted `api/render/engine.ts` and `api/render/status.ts`; loopback request, query-encoding, envelope, transition, and terminal-state coverage in `.agents/skills/gestaltview-gen-render-engine/tests/test_submit_job.py`. | **Verified locally:** endpoint and helper-envelope parity only. Deployment, provider availability, durable persistence, and live reachability were not tested. |
| 2026-07-17 | The submission helper limits request files to 1 MiB and API responses to 2 MiB, requires an HTTP(S) origin, validates polling arguments and submission receipts, uses explicit 30-second request timeouts, accepts authentication only from a named environment variable, and maps file, HTTP, connection, decoding, and size failures to stable messages that omit tokens, request paths, response bodies, hosts, and provider details. | Standard-library loopback and subprocess security regressions in `.agents/skills/gestaltview-gen-render-engine/tests/test_submit_job.py`; local source inspection of the promoted helper. | **Verified locally:** helper safety behavior only; these are not rooted API limits or evidence of live service security. |

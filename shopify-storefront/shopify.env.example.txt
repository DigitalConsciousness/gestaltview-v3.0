# Approved public identities. These remain server-side so configuration can fail
# closed if a deployment points at another offer.
SHOPIFY_STORE_DOMAIN=vercel-store-fa16b8a7-rc5epai7.myshopify.com
SHOPIFY_PRODUCT_GID=gid://shopify/Product/8985408208975
SHOPIFY_VARIANT_GID=gid://shopify/ProductVariant/46345021718607
SHOPIFY_STOREFRONT_ACCESS_TOKEN=replace-with-headless-storefront-token

# Canonical GestaltView application used for requisition, orientation, and legal handoffs.
GESTALTVIEW_APP_URL=https://gestaltview-di-gsvw.vercel.app

# Phase 1 public catalog switch. Safe fallback remains available when false.
STOREFRONT_PUBLIC_ENABLED=false

# Keep false until Phase 2 checkout, webhook, fulfillment, and refund gates pass.
STOREFRONT_CHECKOUT_ENABLED=false

# Must belong to the same custom app that owns the three webhook subscriptions.
SHOPIFY_APP_CLIENT_SECRET=replace-with-server-only-app-secret

# Server-only commerce persistence. SUPABASE_SERVICE_ROLE_KEY remains a legacy
# fallback, but new deployments should use SUPABASE_SECRET_KEY.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SECRET_KEY=replace-with-server-only-secret

# Independent high-entropy secret used only for claim and rate-limit hashing.
STOREFRONT_CLAIM_TOKEN_PEPPER=replace-with-at-least-32-random-characters

# Canonical HTTPS storefront origin, with no trailing path.
STOREFRONT_ORIGIN=https://store.example.com

# Cart creation uses the same server-only Storefront token. Enabling checkout means
# the connected store policies, fulfillment, verified webhook, replay, refund, and
# activation receipt gates have been tested—not merely that a button renders.
